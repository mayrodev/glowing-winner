commit-msg:
  commands:
    commitlint:
      run: npx --no -- commitlint --edit {1}

pre-commit:
  commands:
    bump-version:
      run: node scripts/bump-version.mjs

post-commit:
  commands:
    tag-version:
      run: node scripts/tag-version.mjs
